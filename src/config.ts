// Set the port that the app will run on
export const PORT: number = 8080;
export const DB_URL: string = 'mongodb://localhost:27017/d4';
export const NODE_ENV: string = 'development';
export default {
  PORT,
  DB_URL,
  NODE_ENV,
};
